#!/usr/bin/env bash
# Daily collection + extraction. PROVIDER=openrouter|local|none (default none = fetch only).
set -euo pipefail
cd "$(dirname "$0")/.."
PROVIDER="${1:-${PROVIDER:-none}}"
exec swamp workflow run collect-daily --input "provider=$PROVIDER"
