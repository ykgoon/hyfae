#!/usr/bin/env bash
# Collection + extraction. PROVIDER=openrouter|local|none (default local; none = fetch only).
set -euo pipefail
cd "$(dirname "$0")/.."
PROVIDER="${1:-${PROVIDER:-local}}"
exec swamp workflow run collect --input "provider=$PROVIDER"
